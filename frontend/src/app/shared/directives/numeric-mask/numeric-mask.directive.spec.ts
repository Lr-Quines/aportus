import { NumericMaskDirective } from './numeric-mask.directive';

describe('NumericMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new NumericMaskDirective();
    expect(directive).toBeTruthy();
  });
});
